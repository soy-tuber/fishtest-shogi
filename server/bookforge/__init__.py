"""bookforge: distributed shogi opening-book generation on top of fishtest.

The modules in this package are deliberately independent of the fishtest
game-testing code (fastchess, SPRT, SPSA, PGN). Only the generic pieces
(``fishtest.scheduler.Scheduler``) are reused.
"""
